# BroadCastV2

胡老师口播视频批量剪辑桌面端。

## 功能

- 导入批量 JSON 文案。
- 调用蝉镜生成数字人口播 raw。
- 使用模型审查字幕换行。
- 自动渲染标题、字幕、声明和 BGM。
- 支持选择单独行出片。
- 支持编辑标题和内容。
- 支持配置字体、BGM、文字颜色和描边样式。

## 开发

```bash
npm install
npm run dist
```

应用运行时需要在界面设置里填写蝉镜和模型接口配置。请不要提交本地密钥、生成视频、raw 文件或打包产物。
